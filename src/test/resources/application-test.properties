# This profile is activated by @ActiveProfiles("test") in test classes.
# We override the datasource properties dynamically with @DynamicPropertySource,
# but other test-specific properties can go here.

# Ensure Flyway runs on the test database and JPA initialization waits for it.
spring.flyway.enabled=true
spring.jpa.defer-datasource-initialization=true